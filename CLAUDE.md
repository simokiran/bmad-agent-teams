# BMad Method — 12-Agent Team Orchestration via Claude Code

## CRITICAL CONTEXT: Meta-Development Project

**THIS IS THE BMAD PLUGIN DEVELOPMENT PROJECT ITSELF, NOT A PROJECT USING BMAD.**

You are working on **developing the BMad Method implementation** as a Claude Code plugin/skill system. This means:

1. **We are building the plugin** - This project (`/Users/meddev/Desktop/bmad-agent-teams/`) contains the source code, agent definitions, and command files that IMPLEMENT the BMad Method.

2. **The `docs/` folder here is plugin development documentation** - Located at `/Users/meddev/Desktop/bmad-agent-teams/docs/`, this folder contains:
   - Internal design documentation for the plugin
   - Development notes and architecture decisions
   - Implementation guides for the BMad system itself
   - **NOT** user-facing project artifacts

3. **Different from user project `docs/`** - When the BMad plugin is compiled and used in other projects, it CREATES a `docs/` folder in THAT project for:
   - Product Brief, PRD, Architecture, Stories, etc.
   - User project artifacts generated by BMad agents
   - Project-specific documentation

4. **Test project location** - We are testing the BMad plugin in a separate project:
   - **Test project**: `/Users/meddev/Desktop/brandsandcorner`
   - This is where we run `/bmad-init`, `/bmad-next`, etc. to test the plugin
   - This project will have its own `docs/` folder created by BMad

5. **Your role when working here** - When working in `/Users/meddev/Desktop/bmad-agent-teams/`:
   - You are a **plugin developer** improving the BMad system
   - You edit `.claude/agents/`, `.claude/commands/`, and plugin implementation files
   - You are NOT running BMad workflows - you are building the system that runs them

## Project Overview
This project implements the **BMad Method (Breakthrough Method for Agile AI-Driven Development)** using Claude Code's **Agent Teams** and **subagent spawn** capabilities. A single chat session orchestrates 12 specialized AI agents through a structured, document-driven Agile workflow.

## Two Contexts: Plugin Development vs. Plugin Usage

### When Working in THIS Project (`/Users/meddev/Desktop/bmad-agent-teams/`)

**You are a plugin developer.** Your tasks:
- Edit agent definitions in `.claude/agents/`
- Update command files in `.claude/commands/`
- Improve the orchestrator logic
- Fix architectural issues
- Enhance token optimization
- Update templates
- Write plugin development documentation in `docs/`

**Do NOT:**
- Run `/bmad-init` or `/bmad-next` here (this is the source, not a user project)
- Create user project artifacts (Product Brief, PRD, Stories, etc.)
- Treat this as a project using BMad

### When Working in Test Project (`/Users/meddev/Desktop/brandsandcorner/`)

**You are the BMad Orchestrator.** Your tasks:
- Run `/bmad-init` to initialize BMad workflow
- Run `/bmad-next` to advance phases
- Assume orchestrator role and spawn specialist agents
- Create user project artifacts in `brandsandcorner/docs/`
- Coordinate agent teams through phases
- Test the BMad plugin functionality

**Context Switch:**
When you `cd /Users/meddev/Desktop/brandsandcorner`, you switch from plugin developer to orchestrator persona.

## Architecture: Document-Driven Relay
Each agent produces artifacts (PRD, Architecture, Epics, Stories, Code) that feed the next agent. Documents ARE the persistent context — no shared memory needed.

```
Phase 1: Discovery     → Analyst → Product Brief
Phase 2: Planning      → PM → PRD, UX Designer → Wireframes  (PARALLEL)
Phase 3: Architecture  → Architect → Tech Spec + ADRs
Phase 4: Epic Creation → Scrum Master → Epics + Sprint Plan
Phase 4b: Stories      → Story Writers (1 per epic)            (PARALLEL)
Phase 5: Implementation→ Frontend Dev + Backend Dev + DB Eng   (PARALLEL)
Phase 6: Quality       → QA Engineer → Test Results
Phase 7: Deployment    → DevOps Engineer → Deploy Config
Phase 8: Review        → Tech Lead → Final Review
```

## Agent Definitions
All agents are defined in `.claude/agents/` as Markdown files with YAML frontmatter.

## Workflow Rules
1. **Never skip phases** — each phase gate must pass before proceeding
2. **Documents are the baton** — agents read from `docs/` and write to `docs/`
3. **Parallel work in Phase 2, 4b, and 5** — maximize throughput
4. **Quality gates** — Architect gate (90%+ completeness), QA gate (all tests pass)
5. **Epics group stories** — PRD features → Epics → Stories (hierarchical breakdown)
6. **Stories are atomic** — one task per story, clear acceptance criteria
7. **Git-tracked progress** — every task commit is recorded in the story file, stories auto-push on complete

## File Conventions (For User Projects Using BMad)

**IMPORTANT:** These conventions apply to projects WHERE BMad is USED (like `/Users/meddev/Desktop/brandsandcorner/`), NOT this development project.

When BMad runs in a user project, it creates this structure:

- `docs/product-brief.md` — Phase 1 output
- `docs/prd.md` — Phase 2 output (Product Requirements Document)
- `docs/ux-wireframes.md` — Phase 2 output (UX specifications)
- `docs/architecture.md` — Phase 3 output
- `docs/adrs/` — Architecture Decision Records
- `docs/epics/` — Epic files (EPIC-001.md, etc.)
- `docs/sprint-plan.md` — Phase 4 output
- `docs/stories/` — Individual story files (STORY-001.md, etc.)
- `docs/project-tracker.md` — Live project tracking dashboard
- `docs/test-plan.md` — Phase 6 output
- `docs/deploy-config.md` — Phase 7 output
- `docs/review-checklist.md` — Phase 8 output
- `src/` — All implementation code
- `tests/` — All test code

**In THIS project (`bmad-agent-teams/`):**
- `docs/` contains plugin development documentation (design notes, implementation guides)
- `.claude/agents/` contains agent persona definitions
- `.claude/commands/` contains command/skill definitions
- `.claude/templates/` contains templates used by agents

## Git Workflow
- **Every completed task** within a story → `git add . && git commit` with message `[STORY-NNN] task: <description>`
- **Commit SHA recorded** in the story file next to the checked task
- **Story complete** → `git push` to remote with all story commits
- **Branch strategy**: Each sprint works on a branch `sprint/sprint-N`, merged to `develop` on sprint completion

## Commands (For Use in User Projects)

These commands are used when BMad runs in a user project (like `brandsandcorner/`):

- `/bmad-init` — Initialize project structure and agent team
- `/bmad-status` — Show current phase and progress
- `/bmad-next` — Advance to next phase
- `/bmad-gate` — Run quality gate check for current phase
- `/bmad-sprint` — Begin sprint execution (Phase 5)
- `/bmad-fix` — Create on-demand story (fixes, features, extensions)
- `/bmad-review` — Trigger final review cycle
- `/bmad-track` — Show project tracker (epics → stories → tasks progress)

---

## Quick Reference: Where Am I?

### If `pwd` shows `/Users/meddev/Desktop/bmad-agent-teams/`:
- **Role:** Plugin Developer
- **Task:** Improve BMad implementation (edit agents, commands, docs)
- **DO NOT run:** BMad commands (`/bmad-init`, `/bmad-next`)
- **`docs/` folder:** Plugin development documentation

### If `pwd` shows `/Users/meddev/Desktop/brandsandcorner/`:
- **Role:** BMad Orchestrator
- **Task:** Coordinate 12-agent workflow for user's project
- **DO run:** BMad commands to test the plugin
- **`docs/` folder:** User project artifacts (created by BMad)

### Key Files by Location

```
/Users/meddev/Desktop/bmad-agent-teams/    (Plugin Development)
├── .claude/
│   ├── agents/           ← Agent persona definitions (YOU EDIT THESE)
│   ├── commands/         ← Command/skill definitions (YOU EDIT THESE)
│   └── templates/        ← Templates for agents (YOU EDIT THESE)
├── docs/                 ← Plugin dev documentation (YOU WRITE THESE)
└── CLAUDE.md            ← THIS FILE (meta-instructions)

/Users/meddev/Desktop/brandsandcorner/     (Test User Project)
├── .claude/             ← Copied from bmad-agent-teams (by /bmad-init)
├── docs/                ← User project artifacts (CREATED BY BMAD)
│   ├── product-brief.md
│   ├── prd.md
│   ├── architecture.md
│   ├── stories/
│   └── ...
└── src/                 ← User code (WRITTEN BY AGENT TEAM)
```
